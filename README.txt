# Installation
- If there is no virtualenv, create one
    `virtualenv env`
- Activate virtualenv
    `source env/bin/activate`
- Install requirements
    `pip install -r requirements`
- Run with debug
    `python manage.py runserver`